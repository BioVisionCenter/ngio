# ngio.config API reference

The configuration models behind `~/.ngio/ngio_config.json` and
`get_config()`; the [configuration guide](../../getting_started/7_configuration.md)
explains what each section does and when it takes effect.

::: ngio.config
